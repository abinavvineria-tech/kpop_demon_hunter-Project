# lets createe a function in python that takes a list of integers and returns a new list with only the even integers from the original list.def filter_even_integers(int_list):
import os
import sys
os.environ['PYTHONPATH'] = os.path.abspath(
    os.path.join(os.path.dirname(__file__), '..', '..'))
os.system('cls' if os.name == 'nt' else 'clear')\
    # bye
